<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="soccerfriend.mapper.SoccerMatchMapper">

    <insert id="insert" parameterType="soccerfriend.dto.SoccerMatch">
        INSERT
        INTO soccer_match(clubA_score, clubA_score, soccer_match_recruitment_id, created_at, updated_at)
        VALUES (0, 0, #{soccerMatchRecruitmentId}, now(), now())
    </insert>

    <select id="getSoccerMatchById" resultType="soccerfriend.dto.SoccerMatch">
        SELECT id, clubA_score, clubB_score, soccer_match_recruitment_id, created_at, updated_at
        FROM soccer_match
        WHERE id = #{id}
    </select>

    <select id="getSoccerMatchBySoccerMatchRecruitmentId" resultType="soccerfriend.dto.SoccerMatch">
        SELECT id, clubA_score, clubB_score, soccer_match_recruitment_id, created_at, updated_at
        FROM soccer_match
        WHERE soccer_match_recruitment_id = #{soccerMatchRecruitmentId}
    </select>

    <select id="getSoccerMatchByClubId" resultType="soccerfriend.dto.SoccerMatch">
        SELECT a.id, a.clubA_score, a.clubB_score, a.soccer_match_recruitment_id, a.created_at, a.updated_at
        FROM soccer_match a,
             soccer_match_recruitment b ON a.soccer_match_recruitment_id = b.id
        WHERE (b.clubB_id is not null)
          AND (b.clubA_id = #{clubId}
           OR b.clubB_id = #{clubId})
    </select>

    <update id="increaseClubAScore">
        UPDATE soccer_match
        SET clubA_score = clubA_score + 1,
            updated_at  = now()
        WHERE id = #{id}
    </update>

    <update id="increaseClubBScore">
        UPDATE soccer_match
        SET clubB_score = clubB_score + 1,
            updated_at  = now()
        WHERE id = #{id}
    </update>

    <select id="getClubAId" resultType="int">
        SELECT b.clubA_id
        FROM soccer_match a
                 INNER JOIN soccer_match_recruitment b
                            ON a.soccer_match_recruitment_id = b.id
        WHERE a.id = #{id}
    </select>

    <select id="getClubBId" resultType="int">
        SELECT b.clubB_id
        FROM soccer_match a
                 INNER JOIN soccer_match_recruitment b
                            ON a.soccer_match_recruitment_id = b.id
        WHERE a.id = #{id}
    </select>

    <select id="isClubExist" resultType="boolean">
        SELECT EXISTS(
                       SELECT *
                       FROM soccer_match a
                          , soccer_match_recruitment b
                       ON a.soccer_match_recruitment_id = b.id
                       WHERE a.id = #{id}
                           AND (b.clubA_id = #{clubId} OR b.clubB_id = #{clubId})
                   )
    </select>
</mapper>