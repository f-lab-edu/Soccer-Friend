<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="soccerfriend.mapper.PointChargePaymentMapper">

    <insert id="insert" parameterType="soccerfriend.dto.PointChargePayment">
        INSERT
        INTO point_charge_payment(payer_type, payer_id, price, pg, payment_type, created_at, updated_at)
        VALUES (#{payerType}, #{payerId}, #{price}, #{pg}, #{paymentType}, now(), now())
    </insert>
</mapper>